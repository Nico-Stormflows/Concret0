import { MessageCircle, Mail, PenTool } from "lucide-react"
import { ReactNode } from "react"

interface Step {
  icon: ReactNode
  title: string
  description: string
}

export function StepsSection() { return null }
